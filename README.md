# Budget Tracker

Personal weekly cashflow tracker with Firebase persistence.

## Setup

```bash
npm install
npm run dev
```

## Deploy

Push to `main` branch — GitHub Actions automatically builds and deploys to GitHub Pages.

## Firebase

Data is stored in Firestore under authenticated user accounts. Google sign-in only.
